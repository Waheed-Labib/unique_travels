import React from 'react';

const Circulars = ({ originalCirculars }: {
    originalCirculars: string[];
}) => {
    return (
        <div>
            <label className="label font-semibold text-primary">2. Update Circulars</label>
        </div>
    );
};

export default Circulars;